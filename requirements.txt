# SEAM — Social-cost Enhancement for Agent Movement
#
# PyTorch is installed via conda (see README).
# RAILGUN is NOT a pip package — clone it separately:
#   git clone <railgun_repo_url> RAILGUN
#   cd RAILGUN && pip install -e .

# MAPF environment
pogema>=1.4.0

# RL training
pyyaml                 # config loading in train_rl.py
tensorboard            # TensorBoard logging
tensorboardX           # alternative TB writer, useful in notebooks

# Analysis / heuristics
numpy
networkx               # betweenness centrality for heuristic cost
scikit-learn           # logistic regression, AUC for diagnostics
scipy                  # graph / stats utilities

# Data
pandas
pyarrow                # parquet I/O for diagnostic logs

# Visualization / notebooks
matplotlib
seaborn
ipykernel              # run notebooks in conda env
ipywidgets             # progress bars in Jupyter

# Misc
tqdm
gymnasium>=0.26        # pogema depends on this
